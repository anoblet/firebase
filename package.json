{
  "name": "@anoblet/firebase",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "save": "git add . && git commit -m \"Chore: Save\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "rollup --config -w"
  },
  "author": "Andrew Noblet",
  "license": "ISC",
  "dependencies": {
    "firebase": "^7.1.0"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  },
  "types": "dist/index.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
